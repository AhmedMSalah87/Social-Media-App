export enum Gender {
  male = "male",
  female = "female",
}

export enum Provider {
  credentials = "system",
  google = "google",
}

export enum Role {
  user = "user",
  admin = "admin",
}

export enum UserEvents {
  confirmEmail = "confirmEmail",
  forgetPassword = "forgetPassword",
}
